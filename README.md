# SimpleCalculator
A command-line calculator which performs addition, subtraction, multiplication &amp; division for user-defined input. It handles invalid inputs &amp; displays the correct results in terminal.
